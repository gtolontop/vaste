{
  "name": "vaste",
  "version": "1.0.0",
  "description": "Multiplayer voxel game with React client, authentication backend, and Node.js game servers",
  "main": "index.js",
  "scripts": {
    "start:backend": "cd backend && npm start",
  "test": "node server/tests/test_reconnect_resume_e2e.js && node server/tests/test_chunkSerializer_seq.js && node server/tests/test_clientStateManager.js",
    "start:server": "cd server && node server.js",
    "start:client": "cd client && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../server && npm install && cd ../client && npm install",
    "migrate:db": "cd backend && npm run migrate",
    "setup": "npm run install:all && npm run migrate:db"
  },
  "keywords": ["voxel", "multiplayer", "websocket", "react", "three.js", "game", "authentication", "mysql"],
  "author": "",
  "license": "MIT"
}
